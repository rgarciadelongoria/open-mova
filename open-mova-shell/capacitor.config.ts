import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.openmova.shell',
  appName: 'Open Mova Shell',
  webDir: 'dist/browser',
};

export default config;
