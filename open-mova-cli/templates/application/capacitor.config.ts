import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.openmova.app__APPLICATION_PACKAGE_NAME__',
  appName: '__APPLICATION_NAME__',
  webDir: 'dist/browser',
};

export default config;
